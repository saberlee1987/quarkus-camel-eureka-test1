package com.saber.quarkus.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeEurekaClientIT extends EurekaClientTest {

    // Execute the same tests but in native mode.
}